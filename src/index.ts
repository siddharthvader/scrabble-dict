// Import the functions and variables from scrabbleDict.ts
import { check, iterator, children, anagram, _DAWG } from './scrabbleDict';

// Export the functions and variables so that they can be used by consumers of your library
export {
    check,
    iterator,
    children,
    anagram,
    _DAWG
};
